<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.img.equran">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/eq_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/eq_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Help"></activity>
        <activity android:name=".History" />
        <activity android:name=".FreeMins" />
        <activity android:name=".Refrel" />
        <activity android:name=".Fmoney" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".TeacherProfile" />
        <activity android:name=".TeacherRegister" />
        <activity
            android:name=".navigator"
            android:label="@string/title_activity_navigator" />
        <activity android:name=".Register" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>